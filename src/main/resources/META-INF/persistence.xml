<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
                 https://jakarta.ee/xml/ns/persistence
                 https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <persistence-unit name="FormiumPU" transaction-type="RESOURCE_LOCAL">

        <!-- Proveedor JPA / Hibernate -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Entidades explícitas -->
        <class>modelo.producto.ProductoBase</class>
        <class>modelo.producto.ProductoCamiseta</class>
        <class>modelo.producto.ProductoChaqueta</class>
        <class>modelo.producto.ProductoPantalones</class>
        <class>modelo.inventario.Inventario</class>
        <class>modelo.usuario.Usuario</class>
        <class>modelo.pedido.Pedido</class>
        <class>modelo.pedido.ItemPedido</class>

        <!-- Desactiva el escaneo automático -->
        <exclude-unlisted-classes>true</exclude-unlisted-classes>

        <!-- Propiedades -->
        <properties>
            <!-- Conexión H2 (modo archivo) -->
            <property name="jakarta.persistence.jdbc.driver"  value="org.h2.Driver"/>
            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:h2:file:./tienda_db;MODE=LEGACY;DATABASE_TO_LOWER=TRUE;AUTO_SERVER=TRUE"/>
            <property name="jakarta.persistence.jdbc.user"     value="sa"/>
            <property name="jakarta.persistence.jdbc.password" value=""/>

            <!-- Hibernate -->
            <property name="hibernate.dialect"            value="org.hibernate.dialect.H2Dialect"/>
            <property name="hibernate.hbm2ddl.auto"       value="update"/>
            <property name="hibernate.show_sql"           value="false"/>
            <property name="hibernate.format_sql"         value="true"/>
            <property name="hibernate.use_sql_comments"   value="true"/>
        </properties>
    </persistence-unit>
</persistence>
